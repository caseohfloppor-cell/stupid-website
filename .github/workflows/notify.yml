name: Send Phone Notification

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  notify:
    runs-on: self-hosted
    steps:
      - name: Send Claude's Notification
        run: |
          C:\Users\darkg\Downloads\platform-tools-latest-windows\platform-tools\adb.exe shell cmd notification post -S bigtext -t "hey its claude" "ClaudeNotif" "yeah im in ur phone now. anyway hows ur day going lol"
